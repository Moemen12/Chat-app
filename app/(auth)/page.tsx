import Form from "@/components/shared/Form";
import { NextPage } from "next";
import React from "react";

const page: NextPage = (): React.ReactNode => {
  return <Form type={"login"} />;
};

export default page;
